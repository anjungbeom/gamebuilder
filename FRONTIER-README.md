# Drawn Frontier — 기준 프로토타입 문서

이 파일은 sibling `frontier/README.md`의 설계 설명을 별도 이름으로 보존한 문서다. 실제 프로젝트 콘셉트 기록은 기존 `readme.md`를 유지한다.

## 원본 핵심

**그린 것이 곧 도구가 된다.**

플레이어가 화면에 그린 그림의 생김새가 그대로 도구의 성능이 되는 절차 생성 개척 게임이다. 무엇을 그렸는지는 묻지 않고, 어떻게 생겼는지만 잰다.

| 그리는 방식 | 얻는 능력치 | 재는 값 |
|---|---|---|
| 길게 뻗어서 | 사거리 | 가장 멀리 떨어진 두 점 사이 거리 |
| 뾰족하게 꺾어서 | 절단력 | 가장 날카롭게 꺾인 꼭짓점의 각도 |
| 닫아서 | 부력 | 닫힌 획이 감싼 넓이 |
| 획을 여러 개로 | 접지력 | 자유롭게 열린 끝의 개수 |
| 많이 그려서 | 내구도 | 획의 총 길이 |

## 원본 루프

1. 절차 생성된 세계를 걷는다.
2. 바위·덤불·가시덩굴·거목·산비탈·물에 막힌다.
3. `Q`를 눌러 필요한 성질을 가진 도구를 그린다.
4. 도구를 써서 길을 열고 내구도를 소비한다.
5. 신호기와 마을을 찾아 지역을 열고, 크리처와 우두머리를 상대한다.

## 실행

ES 모듈을 사용하므로 HTML 파일을 직접 여는 대신 프로젝트 폴더에서 로컬 서버를 실행한다.

```bash
python3 -m http.server 4173 --bind 127.0.0.1
```

브라우저에서 `http://127.0.0.1:4173/`을 연다. 테스트는 `npm test`로 실행한다.

주요 추가 조작은 `Tab` 장비·수집품, `V` 다음 도전과제, `M` 지도, `P` 패링이다.

## 1.0.0 장비 체계

- 손도구는 그림의 강점에 따라 파쇄형·장대형·방벽형·포획형·균형형으로 분류한다.
- 충격은 보스 부위 파괴, 방어는 패링 판정, 조작은 포획·제압 성향, 효율은 내구도에 반영한다.
- 신발은 같은 그림을 달리기·안정성·내구도·잉크 절약으로 해석한다.
- 펫 도구는 같은 그림을 공격력·지원거리·생존력·제압력으로 다시 해석한다.
- 동행 펫은 `Tab`에서 확인 후 방생할 수 있으며 펫 도구는 다음 동행을 위해 보관한다.

## 원본 구조

```text
src/
  rng.js        해시 · PRNG · value noise · fbm
  geometry.js   획을 도구 능력치로 변환
  world.js      바이옴 · 장애물 · 표석 · 크리처 서식
  creature.js   유전체에서 몸 설계 생성
  behavior.js   크리처 · 주민의 개별 보상함수와 행동 선택
  challenges.js 다음 도전과제 · 완료 해금 · 순환 팁
  equipment.js  장비 유형 · 슬롯별 성능 변환
  environment.js 이동 거리 기반 낮밤 · 날씨 · 체온 · 소음
  render.js     캔버스 도트 렌더링
  game.js       상태 기계 · 입력 · 시뮬레이션 · 저장
tests/
  logic.test.mjs
  boot.test.mjs
```

원본은 외부 패키지 없이 브라우저에서 직접 실행되는 ES 모듈 프로젝트다.
